状态模式：
核心：解决系统中复杂对象的状态转换以及不同状态下行为的封装问题。
结构：
context环境类：
环境类中维护一个state对象，它定义了当前的状态。
State抽象状态类：
ConcreateState具体状态类：
每一个类封装了一个状态对应的行为。

常用场景：
1.银行系统中账号状态的管理。
2.OA系统中公文状态的管理。
3.酒店系统中，房间状态的管理。
4.线程对象各状态之间的转换。